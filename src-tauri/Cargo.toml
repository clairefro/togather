[package]
name = "pair-presence"
version = "0.1.0"
description = "A peer-to-peer telepresence widget for two people"
authors = ["Pair Presence"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-shell = "2"
